services:
  postgres:
    image: pgvector/pgvector:0.8.0-pg16
    environment:
      # 憑證由 repo 根目錄 .env 注入(見 .env.example);未設定則 compose 直接失敗(fail-fast)
      POSTGRES_USER: ${POSTGRES_USER:?set POSTGRES_USER in .env}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: ${POSTGRES_DB:?set POSTGRES_DB in .env}
    ports:
      # host 15432 → 容器 5432。避開本機原生 PostgreSQL(5432);15432 亦避開 Windows
      # WinNAT 動態保留埠範圍(5433-5532 等,`netsh int ipv4 show excludedportrange protocol=tcp`)
      - "15432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7.4-alpine
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
  redisdata:
